"""
FRTB-Lite Sensitivities page placeholder.

The main starter dashboard currently implements tabs in `app.py` so the first
demo is easy to run. As the project grows, move each tab into these page modules
to keep the dashboard code modular.
"""
